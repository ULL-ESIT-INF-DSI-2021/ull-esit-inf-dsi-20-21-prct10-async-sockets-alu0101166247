/* eslint-disable max-len */
import 'mocha';
import {expect} from 'chai';

/**
 * Como no se pide hacer pruebas de esta practica
 * solo puse esta prueba para no crear conflicto
 * con toda la configuracion de coverage
 */

describe('Test:', () => {
  it('True is true', () => {
    expect(true).to.equal(true);
  });
});
